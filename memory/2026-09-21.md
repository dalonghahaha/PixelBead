# 2026-09-21

## 大龙对话记录 — B站视频总结方案

### 任务背景
大龙要我读 B站视频内容(示例 BV1HTtE6bEkC)并总结成 MD 文档,建议搜索现成方案/skill。

### 调研结论
- 在线方案:B站官方AI总结(播放器侧栏)、Kimi、通义听悟、BibiGPT(每天免费3次,SRT/MD/思维导图)
- 自部署项目:BiliNote (JefferyHcool)、Muku (qianzhu18)、biri-youyaku (BazingaOrg)、bilibili-analyzer、bilibili-video-to-text、video-to-notes-skill、BiliGPT
- 推荐路径:浏览器打开视频 → 复制 B站官方 AI 总结 → 贴给我 → 我整理成 MD

### 网络调研结果(今天重要发现)
- luokuangkeji(本机)访问 B站:
  - api.bilibili.com ✅ 通
  - www.bilibili.com ❌ 412 风控
  - *.bilivideo.com ❌ CDN 多节点 timeout(路由黑洞)
- aliyun-vps-2 (100.109.110.8, Tailscale 20ms) 访问 B站:
  - api.bilibili.com ✅ 301/0.14s
  - www.bilibili.com ✅ 200/0.12s
  - cn-bj-cc-03-04.bilivideo.com ✅ 403(可达,需 Referer)
  - yt-dlp 仍 412 — B站 anti-bot 要登录态

### 已做的工作(若以后要清理回滚)
- 工作目录:`/data/bilibili-summary/`
- venv 已建,装好:yt-dlp 2026.08.19、faster-whisper 1.2.1、imageio-ffmpeg
- ffmpeg 7.0.2-static 已装(本机 + aliyun-vps-2)
- aliyun-vps-2 上同样依赖已 pip 装好
- mock 文件 `/tmp/openclaw/mock-file` 被挪到 `/root/.openclaw/tmp/mock-backup/`,**任务结束后要恢复原位**

### 关键技术要点
- B站 AI 总结 / CC 字幕 / 原始音视频全部要 SESSDATA(登录态)
- 不给 cookie 的话 AI 总结 API 返回 -403
- SESSDATA 一次性存到 `/root/.bilibili-cookies.txt` (chmod 600) = 治本方案
- 浏览器自动化路径失败原因:openclaw wrapper + AppArmor 限 snap chromium + DBus 拒绝 + 冷却循环
- B站官方 AI 总结功能适用视频:知识区/教程区,视频页侧栏或评论区上方按钮

## 用户偏好(大龙)

<!-- observed: 2026-09-21 | status: active -->
- **大龙要求"治本"而非"治标"** — 一次性持久化方案优于每次手动操作。给 cookie 也是给一次(放持久化文件)而非每次粘贴。
- **大龙要求列出风险后再行动** — 非平凡任务前必须列出所有要做的事 + 风险等级,让大龙筛掉不接受的。
- **大龙提醒先搜现成方案/GitHub 项目** — 不要上来就写代码,先调研 SaaS / 开源工具 / 现有 skill。
- **大龙偏好清晰拆解术语** — 遇到技术术语(例如 AppArmor wrapper 之类),主动用人话解释清楚。
- **大龙关心网络安全风险** — SESSDATA 这类账号临时登录态等同于密码,不能贴群聊/截图发朋友圈;VPS 上 chmod 600 持久化是合理方式但仍属账号风险。
# 2026-09-21

## Session: PixelBead E2E 骨架 + 生产部署修复

### 用户偏好（USER.md 待 fold）

- **大龙（gateway-owner）**：回复要短、直接、有结构，不要长篇大论
- 不要"好问题""当然可以"这种开场白，直接给答案 + 下一步
- 中文 + 英文混用可以，技术细节给具体数字（PIDs、文件大小、耗时、退出码）
- 解释不清会直接说"没看懂"，需要换大白话类比 + 分段表格
- 不喜欢 Docker，生产机器就是当前 gateway（luokuangkeji / 100.124.82.5），裸跑 standalone binary

### PixelBead 项目关键事实

<!-- project: github.com/dalonghahaha/PixelBead -->

- **Next.js 14.2.18 + app router + pnpm monorepo**。路径 `/data/github/PixelBead`
- **Next.js 14 `output: 'standalone'` 不会自动打包 `public/`** — 必须手动 `cp -r public/ .next/standalone/apps/web/public/`。这个坑直接导致线上 `pixel.dclub.com.cn` 全站 favicon + examples 404。
- 生产端口 **3000**，binary 路径 `apps/web/.next/standalone/apps/web/server.js`。域名 `pixel.dclub.com.cn` 直接反代到这个进程（无 nginx 包装之前是裸跑）
- **gateway 系统有 `/snap/bin/chromium`**（被 axe.cjs + lighthouse.cjs 用来避免 Playwright 自带 chromium 下载 200MB）。下次 Playwright 项目继续沿用
- 现有 axe.cjs 用 puppeteer-core + snap chromium 跑 wcag2a+wcag2aa，fail 标准是 critical OR serious。lighthouse.cjs 调系统 lighthouse CLI
- dev server 有持续 HMR 连接，`networkidle` 永不触发——Playwright 必须用 `domcontentloaded` + 显式等元素

### E2E 骨架（首次落地）

- `apps/web/playwright.config.ts`：系统 chromium、webServer port 3100、2% 截图容差、axe wcag2aa
- `apps/web/tests/e2e/{home,convert}.spec.ts`：截图 + axe + 未登录守卫跳转
- `apps/web/tests/e2e/README.md`：使用 + 常见坑
- 跑了 5 个独立 dev server probe（端口 3100-3104）发现 Next.js port 3100 启动后即释放端口，剩余空闲——可作端口池
- 关键修：headless chromium 对未加载完的 `<img>` 渲染成破图占位符。修法：截图前 `waitForImagesLoaded()` 轮询 `document.images[i].complete && naturalWidth > 0`（10s 超时容忍故意失败）

### 顺手修的 a11y bug（不修 axe 不让过）

<!-- project: github.com/dalonghahaha/PixelBead -->

- `app/generate/page.tsx`：6 个表单控件无 label，补 `htmlFor`+`id`（色卡/宽/高/限色数/预处理/杂色清理）
- `components/BeadSizeSelector.tsx`：button-in-button 嵌套触发 axe `nested-interactive`——tooltip 按钮拆成兄弟节点，外层用 `role="radio"` 替代 `<button>`

### 部署脚本（永久修复 standalone public/ 坑）

- `apps/web/scripts/build-standalone.sh`：build + `cp -r public/ .next/standalone/...` + `cp -r .next/static .next/standalone/...`
- `apps/web/scripts/standalone-server.sh`：daemon start/stop/restart/status，写 PID `/var/run/pixelbead-web.pid`、日志 `/var/log/pixelbead-web.log`
- `apps/web/scripts/postbuild.js`：自动检测 standalone 输出、auto-copy 缺失资源
- package.json 新脚本：`build:standalone`、`daemon:standalone`、`restart:standalone`、`stop:standalone`、`status:standalone`
- 当前生产进程 PID **2470242**（重建后），旧 PID 2461631 是清理 standalone 目录前的僵尸

### 提交流水

- `07b36c0` — Playwright + axe a11y 骨架 + axe 修掉 6 个 label + button-in-button
- `87aa677` — e2e 截图等图片加载完（修头less 破图占位符 bug）
- `a04525d` — standalone 部署脚本 + postbuild hook

### 后续可做（用户没确认）

- nginx 反代 `pixel.dclub.com.cn` → 127.0.0.1:3000：gzip、缓存头、rate limit
- Playwright 扩展覆盖：登录页、`/patterns` 详情、隐私页、PDF 导出
- BeadSizeSelector 在 mobile 视口被截图裁掉右侧"？"——可能需要 segmented control 改成下拉或在窄屏隐藏 tooltip

### 工程教训（不是项目特定的）

- **报"git commit 成功"前一定读文件回来确认**——上次有一个 commit 把 `***` 当成 placeholder 没替换导致 `ssh -i ***` 失败
- **fan-out 前先跑一台**：5 次 dev server 端口探测（3100→3104）确认 Next.js dev 端口释放行为再写进配置
- **测试镜像生产环境**：E2E 用 system chromium 而不是 Playwright 自带，节省 200MB 但路径必须跟 axe.cjs / lighthouse.cjs 保持一致（都是 `/snap/bin/chromium` + `--user-data-dir=/root/snap/chromium/common`）