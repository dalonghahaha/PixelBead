# Tasks — 006 行为埋点 + 错误监控

<!-- generated by speckit-tasks · 2026-09-20 -->

**优先级**:P0 · **预估工日**:~5 · **依赖**:002-user-auth

---

## Phase 1 — Setup

- [ ] T001 注册 `plausible.io` 账号,绑定域名 `pixelbead.app`
- [ ] T002 注册 `sentry.io` 项目(`pixelbead-web` 前端 / `pixelbead-api` 后端)
- [ ] T003 配置 DNS 子域 `stats.pixelbead.app`(隐私代理脚本)
- [ ] T004 安装 npm 依赖:`@sentry/nextjs` / `@sentry/node`
- [ ] T005 配置环境变量:`SENTRY_DSN` / `SENTRY_AUTH_TOKEN` / `PLAUSIBLE_DOMAIN` / `ANALYTICS_SALT`

---

## US1 — 产品迭代决策需要数据

**作为** 老孙 **我想要** 在 Plausible 看到北极星指标 **以便** 评估功能闭环

**独立测试**:访问 Plausible 仪表盘 → 看到 7 个核心事件 + 3 个漏斗

### 任务

- [ ] T010 [P] [US1] `apps/web/lib/analytics.ts`:Plausible 事件封装函数
- [ ] T011 [US1] 10 个核心事件触发:`pb_auth_signup` / `pb_auth_login` / `pb_generate_start` / `pb_generate_success` / `pb_generate_failure` / `pb_export_pdf` / `pb_export_usage_xlsx` / `pb_export_usage_csv` / `pb_share_click` / `pb_landing_cta_click`
- [ ] T012 [US1] 事件属性:`user_id_hash`(SHA256(email+salt))/ `pattern_id` / `bead_size` / 渠道字段
- [ ] T013 [US1] Plausible 漏斗配置:生成漏斗 / 导出漏斗 / 注册转化漏斗
- [ ] T014 [US1] `apps/web/app/layout.tsx` 挂载 Plausible script(域名 `stats.pixelbead.app`)
- [ ] T015 [US1] 自动化测试:自动化注入 10 个事件,验证 Plausible 接收

**验收**:Plausible 仪表盘可见 10 个事件 + 3 个漏斗,数据完整

---

## US2 — 用户报错排查

**作为** 老孙 **我想要** 在 Sentry 看到前端 + 后端错误堆栈 **以便** 5 分钟内定位根因

**独立测试**:手动注入测试错误 → Sentry 收到堆栈 + 面包屑 + user_id_hash

### 任务

- [ ] T020 [P] [US2] `apps/web/sentry.client.config.ts`:Sentry 浏览器初始化
- [ ] T021 [P] [US2] `apps/web/sentry.server.config.ts`:Sentry SSR 初始化
- [ ] T022 [US2] `apps/web/components/ErrorBoundary.tsx`:根 + 局部 ErrorBoundary(关键路径:生成 / 导出)
- [ ] T023 [US2] `apps/web/components/SentryUserContext.tsx`:注入 user_id_hash + locale
- [ ] T024 [US2] `apps/api/src/core/sentry.py`:Sentry SDK 初始化
- [ ] T025 [US2] `apps/api/src/core/middleware.py`:异常中间件 + `capture_exception`
- [ ] T026 [US2] 关键路径手动上报:生成失败 / 导出失败 / 限流触发(010/008 协同)
- [ ] T027 [US2] Source Map 上传:CI `.sentryclirc` + webpack plugin,绑定 Git SHA 短码
- [ ] T028 [US2] 自动化测试:注入测试错误,验证 Sentry 接收 + 堆栈可读

**验收**:错误捕获率 ≥ 95%,堆栈可读(Source Map 生效),user context 完整

---

## US3 — 真实用户性能监控

**作为** 老孙 **我想要** 在 Sentry Performance 看 LCP / FID / CLS **以便** 优化移动端

**独立测试**:访问 `/generate` → Sentry Performance 有事务记录

### 任务

- [ ] T030 [US3] Sentry Performance 启用(前后端)
- [ ] T031 [US3] 前端页面事务:`/` / `/generate` / `/patterns`
- [ ] T032 [US3] 前端 API 事务:`POST /patterns` / `GET /palettes`(007/008/010/011 协同)
- [ ] T033 [US3] 后端 API 自动 instrumentation(FastAPI)
- [ ] T034 [US3] 采样率配置:生产 10%,开发 100%
- [ ] T035 [US3] 验证 P50/P75/P95 分位数可查

**验收**:P75 LCP 数据可查,浏览器/OS/地区切片可用

---

## US4 — 隐私合规

**作为** 欧盟用户 **我想要** 无 cookie banner 访问 **以便** 不被追踪困扰

**独立测试**:访问站点 → 无 cookie banner,数据匿名化

### 任务

- [ ] T040 [P] [US4] `apps/web/app/privacy/page.tsx`:隐私政策页
- [ ] T041 [US4] 隐私政策明列:Plausible / Sentry + 数据流向 + 用户权利
- [ ] T042 [US4] 用户数据权利:导出/删除申请邮箱(老孙手动响应)
- [ ] T043 [US4] 自动化测试:全站无 cookie 写入(除 session cookie)
- [ ] T044 [US4] GDPR 合规:Plausible 模式无 cross-site tracking

**验收**:无 cookie banner,隐私政策清晰,自动化测试通过

---

## Phase 5 — Polish & Cross-Cutting

- [ ] T050 [P] Sentry Alert:fatal 错误 5 分钟内 ≥ 3 次 → 邮件通知
- [ ] T051 Plausible Uptime Monitoring(可选):站点宕机 1 分钟告警
- [ ] T052 文档:`docs/monitoring.md`(运维 + 工具说明)
- [ ] T053 008 协同:`pb_export_pdf` 成功/失败埋点
- [ ] T054 010 协同:`pb_task_created` / `pb_task_started` / `pb_task_completed` / `pb_task_failed` / `pb_task_cancelled`
- [ ] T055 011 协同:`pb_usage_table_viewed` / `pb_usage_export_xlsx` / `pb_usage_export_csv` / `pb_usage_api_called`
- [ ] T056 Lighthouse Best Practices ≥ 95(埋点不拖分)
- [ ] T057 005 协同:user context 添加 locale 维度

---

## Dependencies

```
T001-T005 (Setup)
  ↓
T010-T015 (US1 埋点)
  ↓
T020-T028 (US2 错误)
  ↓
T030-T035 (US3 性能)
  ↓
T040-T044 (US4 隐私)
  ↓
T050-T057 (Polish + 协同)
```

## 验证清单

- [ ] Plausible 10 个事件可查 ✓
- [ ] Plausible 3 个漏斗配置完成 ✓
- [ ] Sentry 错误捕获率 ≥ 95% ✓
- [ ] Sentry Performance P75 LCP 可查 ✓
- [ ] 无 cookie banner ✓
- [ ] 隐私政策完整 ✓
- [ ] Lighthouse BP ≥ 95 ✓
- [ ] 008/010/011/005 协同埋点全部生效 ✓
---

## Gap Tasks(spec-kit-converge 2026-09-20 发现)

**关键集成缺口(P0)**:事件函数已写但**未挂载到触发点**,埋点全失效。

### Phase 7 — 事件挂载(P0)

- [ ] **G-006-1** `apps/web/app/layout.tsx` 调 `initPlausible()` + `initSentryBrowser()`(挂载到根布局)
- [ ] **G-006-2** `apps/web/app/layout.tsx` 用 `ErrorBoundary` 包裹 `children`
- [ ] **G-006-3** `apps/web/app/generate/page.tsx` 挂载:
  - `submit` 前 `trackGenerateStart(imageSizeKb, beadSize)`
  - 成功后 `trackGenerateSuccess(durationMs, patternId, beadCount)`
  - 失败后 `trackGenerateFailure(errorCode, durationMs)`
- [ ] **G-006-4** `apps/web/components/ExportPdfPanel.tsx` 成功下载后 `trackExportPdf(patternId, pageCount, pdfSizeKb)`
- [ ] **G-006-5** `apps/web/components/UsageTable.tsx` 加载时 `trackUsageTableViewed`,下载时 `trackExportUsageXlsx/Csv`
- [ ] **G-006-6** `apps/web/components/TaskStatusPoller.tsx` complete 时 `trackTaskCompleted`,failed 时 `trackTaskFailed`
- [ ] **G-006-7** `apps/web/app/page.tsx` CTA 点击 `trackLandingCtaClick('hero')`
- [ ] **G-006-8** `apps/web/app/auth/login/page.tsx` 成功 `trackAuthLogin`
- [ ] **G-006-9** `apps/web/app/auth/register/page.tsx` 成功 `trackAuthSignup`

### Phase 8 — 后端埋点(P0)

- [ ] **G-006-10** `apps/api/app/main.py` 全局异常 handler + middleware 调用 `capture_exception`(注入到 FastAPI exception_handler)
- [ ] **G-006-11** `apps/api/app/routers/patterns.py` 关键路径手动上报:`pb_generate_failure` 失败分类
- [ ] **G-006-12** `apps/api/app/routers/tasks.py` 任务失败/完成事件同步埋点(可选 — 当前由前端 track)

### Phase 9 — 隐私与部署(P1)

- [ ] **G-006-13** `apps/web/app/privacy/page.tsx` 新建隐私政策页(Plausible + Sentry 工具清单 + 数据流向 + 联系方式)
- [ ] **G-006-14** `docker-compose.yml` 加 sentry 服务(可选云版跳过)
- [ ] **G-006-15** `.env.example` 加 `SENTRY_DSN` / `NEXT_PUBLIC_SENTRY_DSN` / `PLAUSIBLE_DOMAIN` / `NEXT_PUBLIC_ANALYTICS_SALT`

### Phase 10 — 测试(P1)

- [ ] **G-006-16** `apps/web/tests/analytics.test.ts` 单元测试(mock `window.plausible`)
- [ ] **G-006-17** `apps/web/components/ErrorBoundary.test.tsx` 错误捕获测试

**结论**:功能完整但未挂载。G-006-1 ~ G-006-12 是必须的,否则埋点完全失效。
