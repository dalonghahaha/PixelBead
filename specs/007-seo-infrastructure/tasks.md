# Tasks — 007 SEO 基建

<!-- generated by speckit-tasks · 2026-09-20 -->

**优先级**:P0 · **预估工日**:~7 · **依赖**:005-i18n / 003-image-to-bead-pattern

---

## Phase 1 — Setup

- [ ] T001 创建 `apps/web/lib/seo.ts`:SEO meta 生成函数(title / description / canonical / og)
- [ ] T002 创建 `apps/web/lib/canonical.ts`:canonical URL 工具(去查询参数)
- [ ] T003 数据库迁移:`ALTER TABLE patterns ADD COLUMN is_public BOOLEAN DEFAULT FALSE + public_at TIMESTAMP + og_image_url VARCHAR(255)`
- [ ] T004 创建索引:`CREATE INDEX idx_patterns_public ON patterns(is_public, public_at)`
- [ ] T005 安装 Python 依赖:`Pillow`(OG 图生成)

---

## US1 — 公开图纸被搜索引擎收录

**作为** Google 爬虫 **我想要** 通过 sitemap 发现新公开图纸 **以便** 索引入库

**独立测试**:访问 `/sitemap.xml` → 看到静态页 + 公开图纸,XML 合法

### 任务

- [ ] T010 [P] [US1] `apps/web/app/sitemap.xml/route.ts`:动态生成 sitemap.xml
- [ ] T011 [P] [US1] `apps/api/src/routes/sitemap.py`:服务端生成 + Redis 1 小时缓存
- [ ] T012 [US1] sitemap 包含:静态页(`/`, `/generate`, `/patterns`, `/login`, `/register`, `/privacy`, `/terms`)
- [ ] T013 [US1] sitemap 包含:公开图纸(`/patterns/{id}`,仅 `is_public=true`)
- [ ] T014 [US1] 每条 URL 含 `<loc>` `<lastmod>` `<changefreq>` `<priority>`
- [ ] T015 [US1] 多语言子节点:`<xhtml:link rel="alternate" hreflang="...">`
- [ ] T016 [US1] 分片策略:>50,000 自动分片 + `sitemap-index.xml`
- [ ] T017 [US1] 自动化测试:XML Schema 验证 + Google Search Console 验证

**验收**:sitemap XML 合法,公开图纸被收录

---

## US2 — 社交分享卡片正确

**作为** 用户 **我想要** 分享到 Twitter 看到大图卡片 **以便** 朋友圈/社交曝光

**独立测试**:访问 `/patterns/{id}` → og:image 实际尺寸 ≥ 1200×630

### 任务

- [ ] T020 [P] [US2] `apps/api/src/services/og_generator.py`:Pillow OG 图生成(1200×630)
- [ ] T021 [P] [US2] `apps/api/src/routes/og.py`:OG 图端点 + 磁盘缓存 + Nginx 缓存
- [ ] T022 [US2] OG 图内容:左侧图纸 + 右上 logo + 底部报价
- [ ] T023 [US2] 站点默认 OG 图:`/og/default.png`(1200×630,Pillow 生成)
- [ ] T024 [US2] 公开图纸 OG 图:`/og/{pattern_id}.png`,图纸公开时异步生成
- [ ] T025 [US2] `apps/web/components/OgMeta.tsx`:OG meta 标签组件
- [ ] T026 [US2] `generateMetadata` 注入 `og:title` / `og:description` / `og:image` / `og:url` / `og:type`
- [ ] T027 [US2] `twitter:card` = `summary_large_image`
- [ ] T028 [US2] 自动化测试:Twitter Card Validator / Facebook Sharing Debugger 验证

**验收**:OG 图 ≥ 1200×630,Twitter/FB 验证通过

---

## US3 — robots.txt 正确

**作为** 爬虫 + 站长工具 **我想要** robots.txt 明确允许/禁止规则 **以便** 合规抓取

**独立测试**:访问 `/robots.txt` → 看到正确规则 + sitemap 引用

### 任务

- [ ] T030 [US3] `apps/web/app/robots.txt`:静态文件
- [ ] T031 [US3] 内容:`User-agent: *` + `Allow: /` + `Disallow: /admin/` + `Disallow: /api/` + `Disallow: /account/` + `Sitemap: https://pixelbead.app/sitemap.xml`
- [ ] T032 [US3] 在线验证工具测试

**验收**:robots.txt 内容正确

---

## US4 — JSON-LD 结构化数据

**作为** 搜索引擎 **我想要** JSON-LD 标注页面内容 **以便** 丰富搜索结果

**独立测试**:页面源码含 `<script type="application/ld+json">` + Schema.org 验证通过

### 任务

- [ ] T040 [P] [US4] `apps/web/components/JsonLd.tsx`:JSON-LD 渲染组件
- [ ] T041 [US4] 首页:`SoftwareApplication` schema(name / description / url / applicationCategory / operatingSystem / offers)
- [ ] T042 [US4] 公开图纸页:`CreativeWork` schema(name / author / datePublished / image / keywords)
- [ ] T043 [US4] (可选)组织信息:`Organization` schema(name / url / logo / sameAs)
- [ ] T044 [US4] JSON-LD 通过 `<script type="application/ld+json">` 注入 `<head>`,SSR 友好
- [ ] T045 [US4] Schema.org 验证工具测试 0 错误

**验收**:JSON-LD 0 错误,Schema.org 验证通过

---

## US5 — 与 005 协同多语言 SEO

**作为** 海外爬虫 **我想要** 看到 `<html lang>` + `hreflang` **以便** 多语言 SEO

**独立测试**:爬取 `/zh` `/en`,看到双向 `hreflang`

### 任务

- [ ] T050 [US5] 005 协同:`alternates.languages` 双向链接(`zh-CN` ↔ `en`)
- [ ] T051 [US5] sitemap 含 `<xhtml:link rel="alternate" hreflang="...">` 子节点
- [ ] T052 [US5] 验证每页面 `<html lang>` 正确
- [ ] T053 [US5] 在线验证工具测试 hreflang

**验收**:hreflang 双向链接 100% 存在

---

## Phase 6 — Polish & Cross-Cutting

- [ ] T060 [P] `generateMetadata` 每页面 title 独立(50-60 字符)
- [ ] T061 `generateMetadata` 每页面 description 独立(150-160 字符)
- [ ] T062 `<meta name="viewport">` + `<meta charset>` + `<meta name="robots">` 基础 meta
- [ ] T063 canonical 去重,带查询参数指向无参版本
- [ ] T064 017 协同:分享链接的 OG 继承 007 标准
- [ ] T065 公开图纸 OG 自动跟随 007 标准
- [ ] T066 Lighthouse SEO = 100
- [ ] T067 Google Search Console(若有)提交 sitemap

---

## Dependencies

```
T001-T005 (Setup)
  ↓
T010-T017 (US1 sitemap)
  ↓
T020-T028 (US2 OG)
  ↓
T030-T032 (US3 robots)
  ↓
T040-T045 (US4 JSON-LD)
  ↓
T050-T053 (US5 多语言) ← 005 协同
  ↓
T060-T067 (Polish + 协同)
```

## 验证清单

- [ ] sitemap.xml XML 合法 ✓
- [ ] robots.txt 内容正确 ✓
- [ ] OG 图 ≥ 1200×630 ✓
- [ ] JSON-LD 0 错误 ✓
- [ ] Lighthouse SEO = 100 ✓
- [ ] Twitter/FB 验证通过 ✓
- [ ] hreflang 双向链接 ✓
- [ ] 005/017 协同完整 ✓
---

## Gap Tasks(spec-kit-converge 2026-09-20 发现)

**关键缺口(P0)**:sitemap 路由已实现,但公开图纸 API 端点缺失(`/patterns/public`),sitemap 暂用 TODO 占位。

### Phase 7 — DB 迁移 + API 闭环(P0)

- [ ] **G-007-1** `apps/api/scripts/migrate_pattern_seo.py` 新建:
  - `ALTER TABLE patterns ADD COLUMN is_public BOOLEAN DEFAULT FALSE`
  - `ALTER TABLE patterns ADD COLUMN public_at TIMESTAMP NULL`
  - `ALTER TABLE patterns ADD COLUMN og_image_url VARCHAR(255) NULL`
  - `CREATE INDEX idx_patterns_public ON patterns(is_public, public_at)`
- [ ] **G-007-2** `apps/api/app/routers/patterns.py` 新增 `GET /patterns/public` 端点(返回 `is_public=true` 列表,仅返回 id + updated_at)
- [ ] **G-007-3** `apps/web/app/sitemap.xml/route.ts` 改用真实 `/patterns/public` 端点(去掉 TODO)

### Phase 8 — 前端 SEO 集成(P0)

- [ ] **G-007-4** `apps/web/app/page.tsx` 加 `<JsonLdSoftwareApp />` + `generateMetadata` 加 og:* + twitter:card + canonical
- [ ] **G-007-5** `apps/web/app/patterns/[id]/page.tsx` 加 `<JsonLdCreativeWork />` + og:image(挂载 G-008-1 + G-011-3 同步)
- [ ] **G-007-6** `apps/web/app/og/default.png` 生成(1200×630 站点默认 OG,Pillow)

### Phase 9 — 公开图纸切换(P1)

- [ ] **G-007-7** `apps/api/app/routers/patterns.py` 加 `PATCH /patterns/{id}/visibility`(切换 `is_public`)
- [ ] **G-007-8** `apps/web/app/patterns/[id]/page.tsx` 加"公开图纸"切换按钮
- [ ] **G-007-9** (Phase 3)公开图纸库 — 留作 017

**结论**:sitemap 路由可用,但数据源缺失 + 前端未挂 JSON-LD / OG meta。
