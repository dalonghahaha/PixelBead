# Requirements Quality Checklist — 010 异步生成任务队列

<!-- generated by speckit-checklist · 2026-09-20 -->

## 1. Spec 完整性

- [x] **Overview** 描述同步生成风险 + 引入异步队列的必要性
- [x] **User Scenarios** 6 个核心场景:大图异步 / 小图同步 / 失败重试 / 并发公平 / 结果保留 / 主动取消
- [x] **Functional Requirements** 10 条 FR,覆盖路由 / 任务创建 / 状态查询 / 队列实现 / 生命周期 / 取消 / 重试 / 错误 / 限流 / 监控
- [x] **Success Criteria** 定量 + 定性齐备
- [x] **Key Entities** Task / TaskStatus / TaskError
- [x] **Assumptions** 列出依赖(003 函数无状态 / Redis / 服务器 RAM)
- [x] **Out of Scope** 8 项排除,引用宪法 §5 对应条款
- [x] **Clarifications** 8 个模糊点已决

## 2. WHAT/WHY 纪律

- [x] FR 不含实现细节:无 RQ 库名 / 无 Celery 库名(决策点,在 Clarifications 说明)
- [x] FR 描述需求不描述方案(队列框架选型为决策点)
- [x] 阈值 80×80 为经验值,在 Clarifications 说明可配置

## 3. 可测试性

- [x] FR-1:阈值路由可单元测试(根据格子数决定同步/异步)
- [x] FR-2:任务创建返回 < 200ms 可性能测试
- [x] FR-3:状态查询 < 100ms 可性能测试
- [x] FR-5:状态机转换可状态机测试
- [x] FR-6:取消可注入 running 状态验证
- [x] FR-7:重试上限可注入连续失败验证
- [x] FR-9:限流可注入超过并发数验证

## 4. 可衡量性

- [x] 任务创建 P95 < 200ms
- [x] 状态查询 P95 < 100ms
- [x] 大图完成时间 P95 < 30s
- [x] Worker 并发数可配置(默认 2)
- [x] 重试上限 3 次
- [x] 队列深度上限 100

## 5. 宪法一致性

- [x] §4.3 开源:RQ(BSD)优先
- [x] §4.4 轻量:单进程 RQ 足够,不做分布式
- [x] §4.5 诚实:失败错误分类清晰 + 用户提示
- [x] §5.2:无 AI 任务队列 / 无 DAG 工作流
- [x] §5.4:不复活 014 进度跟踪(物理拼豆本身是进度)

## 6. 协同关系

- [x] 与 003 协同:同步路径完全保留
- [x] 与 006 协同:5 个 task 埋点事件已定义
- [x] 与 009 协同:任务携带 `bead_size` 参数
- [x] 与 011 协同:任务完成后图纸入库,011 可查用量

## 7. 风险与边界

- [x] 阈值 80×80 经验值 + 可配置
- [x] Worker 并发默认 2 + 可配置
- [x] 队列深度上限 + 限流提示
- [x] 取消最终一致性(不强杀 worker)
- [x] 任务结果保留 30 天(任务元数据,非图纸)

## 8. 单 spec 范围

- [x] 仅任务队列调度,不含分布式 / 优先级队列 / DAG
- [x] 实时进度条(细粒度)留作算法 P3+
- [x] 任务结果持久化到 OSS 不做(仍属 003/017)

## 自检结论

✅ **通过** — spec 内容完整,同步/异步路由清晰,失败处理 + 限流 + 取消机制完整,跨多个 spec 协同明确。可进入 plan 阶段(plan 阶段需重点决策:RQ vs Celery 选型 + Redis 部署)。